[project]
name = "vortex"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Michael Luck", email = "56007160+MLucky518@users.noreply.github.com" }
]
requires-python = ">=3.12"
dependencies = [
    "mavsdk>=3.17.2",
    "python-dotenv>=1.2.3",
]

[project.scripts]
vortex = "vortex:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
